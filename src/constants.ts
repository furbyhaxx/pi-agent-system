/** Extension package npm name. */
export const PACKAGE_NAME = "@furbyhaxx/pi-agent-system";

/** Extension command namespace without the leading slash. */
export const COMMAND_PREFIX = "system-prompt";

/** Relative directory for user-editable system prompt partials. */
export const USER_PARTIALS_DIR = "system-prompt/partials";

/** Bundled default system prompt template path relative to package root. */
export const DEFAULT_TEMPLATE_RELATIVE_PATH = "templates/default.SYSTEM.md";
